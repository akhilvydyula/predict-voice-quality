export const layout = {
  /** Max readable width for phone-first screens on desktop web. */
  maxContentWidth: 680,
  /** Marketing landing page width. */
  maxLandingWidth: 960,
  /** App shell main content max width. */
  maxAppContentWidth: 1200,
  /** Left sidebar width on desktop web. */
  sidebarWidth: 248,
  /** Show sidebar instead of bottom tabs at this width (web). */
  sidebarBreakpoint: 1024,
  /** Segmented control width cap. */
  maxSegmentedWidth: 360,
  /** Primary CTA width cap inside cards. */
  maxActionWidth: 320,
} as const;
