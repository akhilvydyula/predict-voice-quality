/** Default export for legacy imports — prefer `useThemeColors()` in UI code. */
export { lightColors as colors, lightGradients as gradients } from './palettes';
export type { ThemeColors, ThemeGradients, ThemeMode } from './palettes';
